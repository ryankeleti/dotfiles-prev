#!/bin/bash

#neofetch --disable title distro icons resolution gpu theme wm font colors --color_blocks pff --gtk_shorthand on --line_wrap off --memory_display infobar --cpu_display off infobar | sed 's/^/     /'
#cal | sed 's/^/     /'

clear && printf "\e[3J";

echo \

date
echo \
