#include <stdio.h>
#include <math.h>

#define MAX 1000

int main(void)
{
  for (int i = 1; i <= MAX; ++i) {
    if (sqrt(i) % == ) printf("%d\t\t%g\n", i, sqrt(i));
    else continue;

  }
  return 0;
}

